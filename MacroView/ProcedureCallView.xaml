<ItemsControl x:Class="MacroView.ProcedureCallView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:macroview="clr-namespace:MacroView"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300" ItemsSource="{Binding ArgumentsVM}">
   <!-- TODO make this class a user control which uses an items control internally -->
   <ItemsControl.ItemsPanel>
      <ItemsPanelTemplate>
         <StackPanel Orientation="Horizontal"/>
      </ItemsPanelTemplate>
   </ItemsControl.ItemsPanel>
   <ItemsControl.Template>
      <ControlTemplate>
         <Border Background="#FFDEF7DA" BorderBrush="#FFABEC92" CornerRadius="5" BorderThickness="3">
            <StackPanel>
               <StackPanel Orientation="Horizontal">
                  <Image Source="/MacroView;component/Resources/ProcedureCallIcon.png" Width="24" Height="24"/>
                  <macroview:MacroBaseView DataContext="{Binding ProcedureVM}" FontWeight="Bold" Margin="5 0 0 0"/>
               </StackPanel>
               <ItemsPresenter Margin="24 0 0 0"/>
            </StackPanel>
         </Border>
      </ControlTemplate>
   </ItemsControl.Template>
</ItemsControl>
