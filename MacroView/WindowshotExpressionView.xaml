<UserControl x:Class="MacroView.WindowshotExpressionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:macroview="clr-namespace:MacroView"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
   <StackPanel>
      <StackPanel Orientation="Horizontal">
         <Image Source="/MacroView;component/Resources/WindowshotExpressionIcon.png" Width="24" Height="24"/>
         <TextBlock Text="("/>
         <macroview:MacroBaseView DataContext="{Binding PositionXVM}"/>
         <TextBlock Text=","/>
         <macroview:MacroBaseView DataContext="{Binding PositionYVM}"/>
         <TextBlock Text=")"/>
         <macroview:MacroBaseView DataContext="{Binding ImageUrlVM}"/>
      </StackPanel>
      <Grid>
         <Image x:Name="_windowshot" Stretch="Uniform" MaxWidth="512" MaxHeight="512" MouseUp="WindowshotMouseUp"/>
         <Border BorderThickness="2" BorderBrush="#E92088E4" x:Name="_kuh">
            <Border.RenderTransform>
               <TranslateTransform x:Name="_muh"/>
            </Border.RenderTransform>
            <Image Source="{Binding Model.ImageUrl.Value}">
            </Image>
         </Border>
      </Grid>
   </StackPanel>
</UserControl>
