<UserControl x:Class="MacroView.SymbolListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:macroview="clr-namespace:MacroView"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
   <StackPanel Orientation="Horizontal" x:Name="_blockStackPanel">
      <Line X1="0" Y1="0" X2="0" Y2="{Binding Path=ActualHeight, ElementName=_blockStackPanel}" Stroke="DarkGray" StrokeThickness="3" StrokeDashArray="2 1 1 1 1 1 1 1"/>
      <ItemsControl ItemsSource="{Binding SymbolsVM}" Background="Transparent" BorderThickness="0">
         <ItemsControl.ItemTemplate>
            <DataTemplate>
               <macroview:MacroBaseView/>
            </DataTemplate>
         </ItemsControl.ItemTemplate>
      </ItemsControl>
   </StackPanel>
</UserControl>
