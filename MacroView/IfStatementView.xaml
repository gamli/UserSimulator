<UserControl x:Class="MacroView.IfStatementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:macroview="clr-namespace:MacroView"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
   <Border Background="#FFF7E5DA" CornerRadius="5" BorderBrush="#FFF0AFF1" BorderThickness="3">
      <StackPanel>
         <macroview:FunctionCallView FunctionName="IF" DataContext="{Binding}">
            <macroview:MacroBaseView DataContext="{Binding ExpressionVM}"/>
         </macroview:FunctionCallView>
         <macroview:MacroBaseView DataContext="{Binding BodyVM}" Margin="5, 0, 0, 0"/>
      </StackPanel>
   </Border>
</UserControl>
