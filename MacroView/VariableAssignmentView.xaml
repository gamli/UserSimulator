<UserControl x:Class="MacroView.VariableAssignmentView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:macroview="clr-namespace:MacroView"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <StackPanel Orientation="Horizontal">
      <TextBox Text="{Binding Model.Symbol}" BorderThickness="0" BorderBrush="Black" Background="Transparent" FontStyle="Italic"/>
      <TextBlock Text="=" Margin="2 0 2 0"/>
      <macroview:MacroBaseView DataContext="{Binding ExpressionVM}"/>
   </StackPanel>
</UserControl>
