<ItemsControl x:Class="MacroView.FunctionCallView"
             x:Name="this"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:system="clr-namespace:System;assembly=mscorlib"
             xmlns:macroview="clr-namespace:MacroView"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300" ItemsSource="{Binding ArgumentsVM}">
   <ItemsControl.ItemsPanel>
      <ItemsPanelTemplate>
         <StackPanel Orientation="Horizontal"/>
      </ItemsPanelTemplate>
   </ItemsControl.ItemsPanel>
   <ItemsControl.Template>
      <ControlTemplate>
         <Border Background="#FFDEF7DA" BorderBrush="#FFABEC92" CornerRadius="5" BorderThickness="3">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
               <macroview:MacroBaseView DataContext="{Binding FunctionVM}" FontWeight="Bold" Margin="0 0 5 0"/>
               <ItemsPresenter/>
            </StackPanel>
         </Border>
      </ControlTemplate>
   </ItemsControl.Template>
</ItemsControl>
