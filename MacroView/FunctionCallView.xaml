<ItemsControl x:Class="MacroView.FunctionCallView"
             x:Name="this"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:system="clr-namespace:System;assembly=mscorlib"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
   <ItemsControl.ItemsPanel>
      <ItemsPanelTemplate>
         <StackPanel Orientation="Horizontal"/>
      </ItemsPanelTemplate>
   </ItemsControl.ItemsPanel>
   <ItemsControl.Template>
      <ControlTemplate>
         <Border Background="#FFDEF7DA" BorderBrush="#FFABEC92" CornerRadius="5" BorderThickness="3">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
               <TextBlock Text="{Binding FunctionName, ElementName=this}" FontWeight="ExtraBlack"/>
               <TextBlock Text="(" FontWeight="ExtraBlack"/>
               <ItemsPresenter/>
               <TextBlock Text=")" FontWeight="ExtraBlack"/>
            </StackPanel>
         </Border>
      </ControlTemplate>
   </ItemsControl.Template>
</ItemsControl>
